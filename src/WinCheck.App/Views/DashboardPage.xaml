<Page
    x:Class="WinCheck.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24">

            <!-- Header -->
            <TextBlock
                Text="System Health Dashboard"
                Style="{StaticResource TitleTextBlockStyle}" />

            <!-- System Health Score -->
            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24">
                <StackPanel Spacing="16">
                    <TextBlock
                        Text="Overall Health Score"
                        Style="{StaticResource SubtitleTextBlockStyle}" />

                    <ProgressRing
                        Width="120"
                        Height="120"
                        Value="82"
                        IsIndeterminate="False" />

                    <TextBlock
                        Text="82/100"
                        Style="{StaticResource HeaderTextBlockStyle}"
                        HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Quick Stats -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- CPU Card -->
                <Border
                    Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE950;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="CPU Usage" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="45%" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock Text="Normal" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Green" />
                    </StackPanel>
                </Border>

                <!-- Memory Card -->
                <Border
                    Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE7C4;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="Memory" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="8.2/16 GB" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock Text="51%" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Green" />
                    </StackPanel>
                </Border>

                <!-- Disk Card -->
                <Border
                    Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE74E;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="Disk Space" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="250/512 GB" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock Text="49%" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Green" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Quick Actions -->
            <StackPanel Spacing="16">
                <TextBlock
                    Text="Quick Actions"
                    Style="{StaticResource SubtitleTextBlockStyle}" />

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Command="{x:Bind ViewModel.QuickScanCommand}"
                           Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE74D;" FontSize="16" />
                            <TextBlock Text="Quick Scan" />
                        </StackPanel>
                    </Button>

                    <Button Command="{x:Bind ViewModel.DeepScanCommand}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE721;" FontSize="16" />
                            <TextBlock Text="Deep Scan" />
                        </StackPanel>
                    </Button>

                    <Button Command="{x:Bind ViewModel.OptimizeCommand}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE713;" FontSize="16" />
                            <TextBlock Text="Optimize" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>

            <!-- Recent Activity -->
            <StackPanel Spacing="16">
                <TextBlock
                    Text="Recent Activity"
                    Style="{StaticResource SubtitleTextBlockStyle}" />

                <ListView>
                    <ListViewItem>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" Foreground="Green" />
                            <TextBlock Text="Cleaned 2.4 GB temporary files" />
                            <TextBlock Text="2 hours ago" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </ListViewItem>
                    <ListViewItem>
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE73E;" Foreground="Green" />
                            <TextBlock Text="Disabled 3 startup programs" />
                            <TextBlock Text="1 day ago" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </ListViewItem>
                </ListView>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
